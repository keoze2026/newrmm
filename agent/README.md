# Endpoint agent

Placeholder. The agent is Phase 2 work: it enrolls with the relay, captures the
endpoint screen, injects mouse and keyboard input, and shows the tray presence
and consent surface. Nothing here runs yet.
